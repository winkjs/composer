// nodes/controller/recompute.js

const recompute = function ( _state ) {
    // Controller has no numerical state to recompute
    // All state is discrete (counters, flags)
    return true;
}; // recompute()

export default recompute;
