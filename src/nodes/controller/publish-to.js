// nodes/controller/publish-to.js

const publishTo = function ( _state, _msg ) {
    // Controller is pure orchestration - it doesn't publish any data
    // Messages pass through unchanged
}; // publishTo()

export default publishTo;
