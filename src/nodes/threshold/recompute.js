// nodes/threshold/recompute.js

const recompute = function () {
    // Nothing to recompute - threshold is stateless except for hysteresis memory
    return true;
}; // recompute()

export default recompute;
