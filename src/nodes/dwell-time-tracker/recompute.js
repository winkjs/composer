// nodes/dwell-time-tracker/recompute.js

const recompute = function () {
    // Nothing to recompute - dwell time tracker is stateless except for timing memory
    return true;
}; // recompute()

export default recompute;
