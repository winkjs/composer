/**
 * @fileoverview Recompute function for emitIf node
 *
 * No numerical computation to stabilize.
 */

const recompute = function ( _state ) {
    // No numerical computation to stabilize
    return true;
}; // recompute()

export default recompute;
