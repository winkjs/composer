const publishTo = function ( _state, _msg ) {
    // Terminal node - nothing to publish for downstream
    // Connection counts are already added in update() before broadcasting
}; // publishTo()

export default publishTo;
